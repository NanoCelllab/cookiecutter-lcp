# analysis/templates — pending marimo conversion

These are the legacy Jupyter starting-point notebooks, kept as placeholders.
The pipeline has since moved to marimo (`.py`) notebooks driven by
`experiment_config.json` (see `workspace/hca_pipeline/`); the generic,
config-driven marimo templates (01 samples retrieval → 07 recovery axis)
will replace these files in a follow-up pass.

For a new experiment, copy whatever lives in this folder into
`workspace/analysis/{EXPERIMENT_ID}/analysis/` and adapt as needed.
